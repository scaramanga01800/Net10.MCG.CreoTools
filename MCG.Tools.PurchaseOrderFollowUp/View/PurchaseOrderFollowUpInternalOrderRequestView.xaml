<Fluent:RibbonWindow x:Class="MCG.Tools.PurchaseOrderFollowUp.View.PurchaseOrderFollowUpInternalOrderRequestView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:local="clr-namespace:MCG.Tools.PurchaseOrderFollowUp.View"
                     xmlns:Fluent="urn:fluent-ribbon"
                     xmlns:MCGWpfLib="clr-namespace:MCG.CommonLib.WpfComponent.View;assembly=MCG.CommonLib.WpfComponent"
                     mc:Ignorable="d"
                     ResizeMode="NoResize"
                     Title="{DynamicResource POF_WindowInternalOrder}"
                     Icon="pack://application:,,,/MCG.CommonLib.Resources;component/Resources/InternalOrder.ico"
                     Height="150" Width="400"
                     Background="{DynamicResource IdealBackgroundColor80}">

    <Window.Resources>
        <ResourceDictionary>
            <BitmapImage x:Key="SEND" UriSource="pack://application:,,,/MCG.CommonLib.Resources;component/Resources/InternalOrderSend.ico" />
            <BitmapImage x:Key="CLOSE" UriSource="pack://application:,,,/MCG.CommonLib.Resources;component/Resources/Delete.ico" />
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Fluent:TextBox Margin="5"
                        Header="{DynamicResource POF_LabelInternalOrderDesc}"
                        Text="{Binding CurrentDataContext.InternalOrderDescription, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"></Fluent:TextBox>

        <StackPanel Orientation="Horizontal"
                    Grid.Row="1"
                    Height="44"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top">
            <Button x:Name="Send"
                    Click="Send_Click">
                <Image Source="{StaticResource SEND}"></Image>
            </Button>
            <Rectangle Width="100"></Rectangle>
            <Button x:Name="CloseWindow"
                    Click="Close_Click">
                <Image Source="{StaticResource CLOSE}"></Image>
            </Button>
        </StackPanel>
    </Grid>
</Fluent:RibbonWindow>
